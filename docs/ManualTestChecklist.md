# ManualTestChecklist.md

> 이 문서는 "Node 테스트로는 못 잡는, 브라우저에서 직접 눈으로 확인해야
> 하는 것"만 모아둔 체크리스트다 (`D-028` 참조).
> domain/store 로직은 `node --test`로 커버되지만, 실제 렌더링·클릭·
> DOM 연결(`ui/*.js`, `index.html`/`output.html`의 embedded script)은
> 자동화 대상이 아니다 — 이 문서가 그 빈틈을 추적한다.
>
> TODO.md와 같은 방식으로 쓴다: 확인되면 체크하고, 무엇을 확인했는지
> 한 줄 남긴다. 오래돼서 더 이상 유효하지 않으면(이후 세션에서 그 위에
> 기능이 문제없이 쌓여 사실상 검증된 경우 등) 이유와 함께 삭제한다.
>
> 최종 업데이트: 2026-07-15 (텍스트 Page 배경 미디어 D-032 E2E 검증 완료)

---

## 확인 필요 (현재 미확인)

- [ ] **Output 창 자동 배치 — 부 모니터 실기기 확인 (P3)** — 헤드리스
  E2E로는 폴백 경로(단일 화면)까지만 검증됨. 다중 모니터 실환경에서
  Output 버튼 클릭 시 (1) window-management 권한 프롬프트 후 output.html이
  부 모니터로 이동/크기 조정되는지, (2) 권한 거부 시 기존 고정 창으로
  무손상 폴백되는지 눈으로 확인 필요.

---

## 확인 완료

- [x] **텍스트 Page 배경 미디어 (D-032)** — 2026-07-15
  Playwright(Chromium) 자동화로 검증, 체크 9건 전부 통과 + JS 에러 0건.
  텍스트 Page 선택 후 라이브러리 "배경으로 적용" → Preview 슬라이드에
  배경 media-layer + text-layer 동시 렌더(배경 img src=blob URL 확인),
  사이드바 배경 row 표시/status "배경"/제거 버튼 노출, 운영 모드
  GO_LIVE 시 output.html에도 배경+텍스트 렌더, "배경 제거" 클릭 시
  Preview media-layer 사라지고 status "없음". e2e-verify 스킬 템플릿
  사용, 스크립트 검증 후 삭제.
- [x] **Output 창 자동 배치 — 폴백 경로 (P3, Window Management API)** — 2026-07-15
  Playwright(Chromium, 단일 화면·권한 없음 = 폴백 경로) 자동화로 검증,
  체크 5건 전부 통과 + JS 에러 0건. Output 버튼 클릭 시 팝업(output.html)
  정상 열림·부팅, 버튼 "송출 중"/broadcasting 클래스 전환, 재클릭 시
  중복 창 미생성(기존 focus 분기 회귀 없음). 부 모니터 자동 배치
  (getScreenDetails→moveTo/resizeTo) 자체는 다중 모니터 실기기 필요라
  헤드리스로 미검증 — 아래 "확인 필요" 참조. e2e-verify 스킬 템플릿
  사용, 스크립트 검증 후 삭제.
- [x] **가사 "N줄씩" 분할 + 미디어 라이브러리 썸네일 (P2 #4/#1a)** — 2026-07-15
  Playwright(Chromium) 자동화로 두 기능 검증, 체크 9건 전부 통과 + JS
  에러 0건. (A) 빈 줄 없는 4줄 + lines-per-slide=2 → Page 2개 생성(경계
  2줄씩), 빈 줄 포함 가사는 빈 줄 기준 분할(lines-per-slide 무시).
  (B) 라이브러리에서 이미지 항목=<img>, 영상 항목=<video>, 각 src가
  blob URL(영상은 #t 프래그먼트). e2e-verify 스킬 템플릿 사용, 스크립트
  검증 후 삭제.
- [x] **Live Emergency Overlay (D-031)** (9-51) — 2026-07-13
  Playwright(Chromium) 자동화로 확인, 체크 11건 전부 통과 + JS 에러
  0건. STANDBY(Live 없음) 위 오버레이 표시, Live 송출과 병행 유지,
  위치 변경(상/하) 즉시 반영, 해제 시 송출 슬라이드 유지, 늦게 연
  output 창의 REQUEST_SYNC 오버레이/슬라이드 수신, Ctrl+Z에 오버레이
  미해제(History Ignore), 컨트롤 is-broadcasting 토글. e2e-verify
  스킬 템플릿 사용, 스크립트는 검증 후 삭제.
- [x] **UI 통지 경로 이행 — storeChanged → Mutation 타겟 통지** (9-46,
  코드는 9-45 이후 커밋 `6cf1730`에 포함) — 2026-07-12
  Playwright(Chromium) 자동화로 사후 확인. 이행된 통지 경로 위에서
  부팅 JS 에러 0건, 가사 추가(SET_PAGES) → CueList 항목 생성, Page
  클릭(SET_SELECTION) → 선택 클래스 부여 + PreviewPanel 내용 반영,
  더블클릭 Live 전환(SET_LIVE_PAGE)/스타일 숫자 입력 변경까지 포함한
  전체 상호작용 후에도 JS 에러 0건. 총 5개 체크 전부 통과. 자동화
  스크립트는 검증 후 삭제.
- [x] **CueList 항목 텍스트 잘림 개선** (9-37) — 2026-07-11
  Playwright(Chromium) 자동화로 확인. 긴 텍스트/미디어 Page 둘 다
  `.cue-preview`의 `title` 속성에 전체 텍스트가 담기고, 실제로 CSS
  ellipsis로 시각 잘림이 발생하는 상황임을 확인. 콘솔 에러 0건, 총
  6개 체크 전부 통과. 자동화 스크립트는 검증 후 삭제.
- [x] **CueList/"초기화" 버튼의 잔존 `confirm()` 제거** (9-36) —
  2026-07-11 Playwright(Chromium) 자동화로 확인. CueList Page 삭제
  1클릭 → "정말 삭제?" 전환(네이티브 dialog 미발생) → 다른 항목 선택
  시 자동 취소 → 2클릭으로 실제 삭제. "초기화" 버튼도 동일하게 1클릭
  → "정말 초기화?" → 2클릭으로 전체 삭제, 3초 안에 다시 안 누르면
  자동 취소까지 확인. 콘솔 에러 0건, 총 10개 체크 전부 통과. 자동화
  스크립트는 검증 후 삭제.
- [x] **Transition** (9-35) — 2026-07-11 Playwright(Chromium) 자동화로
  확인. 메인 탭과 Output 탭을 동시에 띄워 실제 `BroadcastChannel`
  통신까지 검증. STANDBY→Page(`'none'`)는 즉시 표시, Page→Page(`'fade'`)는
  전환 도중 슬라이드 2개가 겹쳐 보였다가 완료 후 1개로 정리, `'none'`
  타입은 대기 없이 즉시 교체, 빠른 연속 전환(첫 페이드 끝나기 전에 다음
  전환 트리거)에도 이전 슬라이드가 leak되지 않고 최종 1개만 남음 — 전부
  정상. 콘솔 에러 0건, 총 9개 체크 전부 통과. 자동화 스크립트는 검증 후
  삭제.
- [x] **스타일 프리셋 — 업데이트(덮어쓰기)** (9-34) — 2026-07-11
  Playwright(Chromium) 자동화로 확인. "찬양 기본" 적용 → 그 값으로 Page
  생성 → 사이드바 값 변경 후 "덮어쓰기" → 성공 토스트 → 새로고침해도
  값 유지 → 다른 프리셋("설교 자막")은 영향 없음 → **덮어쓰기 이전에
  만든 Page를 다시 선택하면 원래 값 그대로**(소급 반영 없음, `D-023`
  확인) 전부 정상. 콘솔 에러 0건, 총 10개 체크 전부 통과. 자동화
  스크립트는 검증 후 삭제.
- [x] **이름 입력 모달 (`showTextPrompt`, 2026-07-11)** — 2026-07-11
  Playwright(Chromium) 자동화로 확인. "+ 섹션" 클릭 → 모달에서 제목 입력
  → 확인 → CueList에 실제로 반영됨. 취소 버튼/Escape 키/바깥 클릭 모두
  정상 취소(모달만 닫힘). Enter 키로 확인 동작. 스타일 프리셋 "새로 저장"도
  같은 모달을 "프리셋 이름" 타이틀로 재사용해 정상 동작. 네이티브
  `prompt()` dialog는 전체 과정에서 한 번도 발생하지 않음(총 17개 체크
  전부 통과). 자동화 스크립트는 검증 후 삭제(D-028 관행).
- [x] **Media Library UI** (9-33) — 2026-07-11 Playwright(Chromium)
  자동화로 확인. Backgrounds/Videos 탭 빈 상태 안내, 업로드 후 목록에
  파일명/용량 표시, 카테고리 분리(이미지↔영상 탭에 안 섞임), "Flow에
  추가" 클릭 시 CueList에 실제 Page 생성 및 토스트 표시, 같은 항목을
  두 번 추가하면 두 개의 Page가 모두 생성됨(mediaId 재사용 확인), 삭제
  2단계 확인 → 실제 삭제 → 모달 재오픈 시 확인 상태가 안 남아있음(Song
  삭제 버그, 9-31과 같은 회귀 패턴 사전 확인) 전부 정상. 콘솔 에러 0건,
  총 18개 체크 전부 통과. 자동화 스크립트는 검증 후 삭제.
- [x] **Song Library UI 전체 CRUD** (9-29) — 2026-07-10 브라우저 확인.
  "+ 새 곡" → 저장 → 목록 표시 → 편집 → 삭제(2단계 확인) 흐름 전부
  이상 없음. 삭제 2단계 확인 상태가 모달 재오픈 시 남아있던 버그
  (`pendingDeleteSongId` 리셋 누락, 2026-07-10 수정 커밋 `a8a8449`)도
  포함해서 재확인, 정상 동작 확인됨.
- [x] **Song → Section → Page 생성 ("Flow에 추가")** (9-30) — 2026-07-10
  브라우저 확인. "Flow에 추가" 클릭 시 CueList에 새 Section(+Page들)
  정상 표시, 이상 없음.
- [x] **Song 재가져오기(Pull)** (9-30) — 2026-07-10 브라우저 확인.
  "⚠ 원본 Song과 달라짐" 표시, "다시 가져오기" 후 표시 해제 + Section
  위치 유지, 원본 Song 삭제 시 안내 토스트까지 전부 이상 없음.

---

## 과거 이력 (참고용, 재확인 우선순위 낮음)

CurrentState.md 9-11~9-27 세션에 "브라우저 실사용 테스트 필요"로
남아있던 항목들이다. TODO.md에서는 전부 `[x]` 완료로 표시돼 있고, 이후
세션들이 그 기능 위에 계속 새 기능을 쌓아왔다(예: Section UI 위에
Song Flow 추가/재가져오기가 9-30에 얹힘) — 문제가 있었다면 그 과정에서
드러났을 가능성이 높아, 사실상 실사용으로 검증된 것으로 간주하고
재확인 우선순위를 낮춘다. 회귀가 의심되는 상황이 아니면 다시 열지
않는다.

- Live Mode 전환 CSS 토글, output.html 송출, Typography 1~4순위 실제
  반영 (CurrentState.md 섹션 8)
- Section Header 생성/접기·펼치기, Section 삭제 토스트 (9-13/9-14/9-22)
- Page 드래그 재정렬, Section 자동 배정 (9-21/9-23)
- Section 순서 위/아래 이동, Flow 순서와 모달 표시 순서 일치 (9-24)
- Section 색상/메모 편집 및 영속 (9-26)
- 스타일 프리셋 적용/저장/삭제 및 새로고침 후 유지 (9-27)

---

## 사용 방법

- 새 기능이 UI/렌더링 계층까지 연결되면, 여기에 항목을 추가한다
  (domain/store 로직만 있는 단계라면 추가하지 않는다 — 그건
  `*.test.js`로 커버).
- 확인되면 체크하고, 무엇을 어떻게 확인했는지 한 줄 남긴다.
- 회귀가 의심되면 "과거 이력"에서 항목을 다시 위로 옮겨 재확인한다.
